[package]
name = "konfide"
version = "0.1.0"
description = "Konfide on-chain program — confidential B2B invoice settlement on Solana."
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib", "lib"]
name = "konfide"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "1.0.2"

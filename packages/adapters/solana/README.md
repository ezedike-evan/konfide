# @konfide/adapter-solana

Typed client for the on-chain Konfide Anchor program (`packages/contracts/programs/konfide`). Wraps `@solana/web3.js@2` and `@coral-xyz/anchor` for the rest of the workspace.

## Status

- [x] `KonfideProgramClient` stub.
- [x] Placeholder IDL type and program id.
- [ ] Real IDL from `anchor build`.
- [ ] Account fetchers, instruction builders.

## Env

- `SOLANA_RPC_URL`
- `SOLANA_PAYER_KEYPAIR_PATH`

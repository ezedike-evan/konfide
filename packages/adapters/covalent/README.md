# @konfide/adapter-covalent

Implements the `ChainData` port from `@konfide/core` using Covalent's unified data API. Powers cross-chain trust scoring.

## Status

- [x] Port stubs in place (`CovalentChainData`).
- [ ] Transfer-history pagination.
- [ ] Cross-chain normalization (Solana ↔ EVM ↔ ...).

## Env

- `COVALENT_API_KEY`

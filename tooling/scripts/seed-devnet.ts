/**
 * Seed the local devnet with fixture counterparties, invoices, and
 * settlements so the dashboard has something to render.
 *
 * Stub: real seeding logic lands once the API + Solana program are live.
 */
import { NotImplementedError } from '@konfide/core'

async function main(): Promise<void> {
  throw new NotImplementedError('seedDevnet')
}

main().catch((err) => {
  // eslint-disable-next-line no-console
  console.error(err)
  process.exit(1)
})

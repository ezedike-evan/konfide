/**
 * Issuer dashboard: list of invoices.
 *
 * Placeholder route — invoice list table lands when API endpoints exist.
 */
import type { ReactElement } from 'react'

export default function InvoicesPage(): ReactElement {
  return (
    <section className="px-6 py-12">
      <h1 className="text-2xl font-semibold">Invoices</h1>
      <p className="mt-2 text-sm text-muted">No invoices yet.</p>
    </section>
  )
}

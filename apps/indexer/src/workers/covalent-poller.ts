/**
 * Worker: poll Covalent for new transfers and feed them into trust scoring.
 * Stub.
 */
import { NotImplementedError } from '@konfide/core'

export async function pollCovalent(): Promise<void> {
  throw new NotImplementedError('pollCovalent')
}
